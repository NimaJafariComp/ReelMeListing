name: listing-to-reel

services:
  dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: ["python", "-m", "listing_to_reel", "environment"]

  api:
    profiles: ["api"]
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    command: ["python", "-m", "listing_to_reel", "environment"]

  worker-cuda:
    profiles: ["worker-cuda"]
    build:
      context: .
      dockerfile: docker/Dockerfile.worker-cuda
    environment:
      LTR_RUNTIME_CONFIG: configs/remote_cuda.yaml
    command: ["python", "-m", "listing_to_reel", "config-check", "--config", "configs/remote_cuda.yaml"]
    gpus: all
