const photos=document.querySelector('#photos'), thumbs=document.querySelector('#thumbs'), button=document.querySelector('#generate'), status=document.querySelector('#status'), duration=document.querySelector('#duration'), transition=document.querySelector('#transition');
const show=()=>{durationOut.value=`${duration.value}s`;transitionOut.value=`${transition.value}s`;thumbs.innerHTML=[...photos.files].map((f,i)=>`<figure><img src="${URL.createObjectURL(f)}" alt="Selected photo ${i+1}"><figcaption>${String(i+1).padStart(2,'0')}</figcaption></figure>`).join('');button.disabled=photos.files.length<2};
photos.addEventListener('change',show);duration.addEventListener('input',show);transition.addEventListener('input',show);
button.addEventListener('click',async()=>{button.disabled=true;status.textContent='Uploading photos…';try{const form=new FormData();[...photos.files].forEach(file=>form.append('files',file));const upload=await fetch('/uploads',{method:'POST',body:form});const uploaded=await upload.json();status.textContent='Rendering reel…';const job=await fetch('/jobs',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({source_paths:uploaded.source_paths,settings:{target_duration_seconds:+duration.value,transition_seconds:+transition.value}})}).then(r=>r.json());let current;do{await new Promise(r=>setTimeout(r,800));current=await fetch(`/jobs/${job.id}`).then(r=>r.json());status.textContent=`${current.status}…`}while(['queued','running'].includes(current.status));if(current.status!=='succeeded')throw Error(current.error);const artifacts=await fetch(`/jobs/${job.id}/artifacts`).then(r=>r.json());const mp4=artifacts.find(a=>a.name.endsWith('.mp4'));player.src=`/jobs/${job.id}/artifacts/${mp4.name}`;download.href=player.src;result.hidden=false;status.textContent='Reel ready.'}catch(error){status.textContent=error.message}finally{button.disabled=false}});show();
